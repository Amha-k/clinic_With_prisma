model Doctor{
    doctorId String @id @map("doctor_id")
    id Employe @relation(fields: [doctorId]  , references: [id])
    specialization String 
    yearOfExpriance Int @map("year_of_expriance")
    medicalRecord MedicalRecord[]
    labRequest LabRequest[]
}